#compdef intercal
# zsh completion for intercal
# Install to any dir in $fpath (e.g., /usr/share/zsh/site-functions/)

_intercal() {
    local state

    _arguments -C \
        '1:source file:_files -g "*.i"' \
        '-o[output path]:output file:_files' \
        && return 0
}

_intercal "$@"
